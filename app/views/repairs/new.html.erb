
<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Quel est votre problème?</h1>
      <p> Nous intervenons en urgence pour réparer les pneus crevés, les chaînes cassées, les freins usées </p>
      <P>Laissez-nous vos coordonées, nous vous rappelons immédiatement </P>
        <%= simple_form_for(@repair) do |f| %>

        <div class="form-for">
        <%= f.error_notification %>
        <h3> <i class="fa fa-bicycle"></i> Vous êtes ici: <i class="fa fa-bicycle"></i> <h3>
        <%= f.input :address, label: false, input_html: {value: @address} %>
        <H3> Choissisez une intervention dans la liste </h3>
        <%=f.input :category, as: :select, label: false, collection: Repair::CATEGORIES, :include_blank => false %>
            <% if @current_user %>
              <%= f.submit "Etre rappelé immédiatement", class: "btn btn-danger cd-btn main-action" %>
            <%else %>
              <%= link_to 'Sign up',  new_user_registration_path, class: "btn btn-danger cd-btn main-action"%>
            <%end%>
        </div>
      <% end %>
      </div>
    </div>
  </div>

